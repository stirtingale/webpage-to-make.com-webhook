// Content script - runs on all pages
// This file is mostly empty as we're using executeScript for dynamic injection
// But it's required by the manifest for the extension to work properly

console.log('Save to Make.com extension loaded');